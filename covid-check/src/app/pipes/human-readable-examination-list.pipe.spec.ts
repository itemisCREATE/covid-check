import { HumanReadableExaminationListPipe } from './human-readable-examination-list.pipe';

describe('HumanReadableExaminationListPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanReadableExaminationListPipe();
    expect(pipe).toBeTruthy();
  });
});
